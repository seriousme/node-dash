{
	"views" : {
		"all" : {
			"map" : "function (doc) {\n  emit(doc._id, 1);\n}"
		},
		"new" : {
			"map" : "function (doc) {\n if (doc.status == \"new\"){\n  emit(doc._id, 1);\n }\n}"
		}
	},
	"_id" : "_design/requests",
}
